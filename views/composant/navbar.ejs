<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Page Title</title>

        <style>
        nav {
            background-color: #333;
            padding: 10px;
            align-content: center;
        }

        nav ul {
            list-style: none;
            display: flex;
            justify-content: space-around; 
            align-items: center;          
            margin: 0;
            padding: 0;
        }

        nav ul li {
            margin: 0;
            font-family: Arial, sans-serif;
            font-size: small;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            padding: 0.5rem 1rem;
            display: block;
        }

        nav ul li a:hover {
            text-decoration: underline;
        }

        /* Dropdown  styles */
        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f3f0f0;
            min-width: 130px;
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
            z-index: 1;
        }

        .dropdown-content a {
            color: black;
            padding: 0.5rem 1rem;
            text-decoration: none;
            display: block;
        }

        .dropdown-content a:hover {
            background-color: #1f1d1d;
            color: white;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }
    </style>
</head>
<body>    
    <nav>
        <ul style="list-style: none; display: flex; justify-content: space-around; margin: 0; padding: 0;">
            <li><a href="/">Acceuil</a></li>
            <li><a href="/liveScore">Pointage en direct</a></li>
            <li><a href="/listeEquipes">Équipes</a></li>
            <li><a href="#service">Statistique</a></li>
            <li><a href="#contact">Classement</a></li>
            <li><a href="#service">Boutique</a></li>

            <!-- Membres Dropdown -->
            <li class="dropdown">
                <!-- if logged in: -->
                <% if (typeof username !== 'undefined') { %> 
                    <a href="#" class="dropdown-link" style="display: flex; align-items: center;">
                        <img src="/images/puck.jpg" alt="User Image" style="width: 30px; height: 30px; border-radius: 50%; margin-right: 8px;" />
                        <span><%= username %></span>
                    </a>
                    <!-- member page+ dropdown options -->
                <div class="dropdown-content">
                <a href="/users/logout">Se déconnecter</a>
                <a href="/users/members">Votre espace</a>
            </div>
                <% } else { %>
                    <a href="#" class="dropdown-link">Membres</a>
                    <div class="dropdown-content">
                        <a href="/users/login">Se connecter</a>
                        <a href="http://localhost:3000/users/signup">Créer un compte</a>
                        <a href="/users/login">Se déconnecter</a>
                    </div>
                <% } %>
            </li>
        </ul>
    </nav>
</body>
</html>
