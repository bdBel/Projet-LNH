<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Page du membre <%= username %></title>

</head>
<style>
   .container {
        
        margin-left: 20px;
    }
    .nav {
        display: flex;
        overflow-x: auto;
        white-space: nowrap;
        background: #ffffff;
        padding: 8px 0;
        border-radius: 8px;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        scrollbar-width: thin;
        scrollbar-color: #d32f2f #f5f5f5;
        z-index: 2000 !important;
        position: relative;
    }

    /* style scrollbar chrome safari edge */
    .nav::-webkit-scrollbar {
        height: 8px;
    }

    .nav::-webkit-scrollbar-track {
        background: #f5f5f5;
        border-radius: 10px;
    }

    .nav::-webkit-scrollbar-thumb {
        background: linear-gradient(to bottom, #d32f2f, #b71c1c);
        border-radius: 10px;
        background: linear-gradient(to bottom, #d32f2f 0%, #b71c1c 100%);
    }

    .nav::-webkit-scrollbar-thumb:hover {
        background: linear-gradient(to bottom, #d32f2f, #b71c1c);
        opacity: 0.8;
    }

    /* style carte match */
    .nav-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 120px;
        padding: 10px;
        background-color: #f9f9f9;
        border-radius: 10px;
        margin: 5px;
        text-align: center;
        font-family: 'Arial', sans-serif;
        font-size: 13px;
        font-weight: bold;
        transition: transform 0.2s ease-in-out;
    }

    .nav-item a {
        font-size: medium;
        text-decoration: none;
        color: rgb(7, 7, 7);
    }

    .nav-item:hover {
        background-color: #f0f0f0;
        transform: scale(1.05);
    }

    /* style logo equipe */
    .nav-item img {
        height: 47px;
        width: 47px;
        object-fit: contain;
        margin-bottom: 5px;
    }
         .modal {
            display: none; 
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5); 
            padding-top: 60px;
        }

          
        .modal-content {
            background-color: #333; 
            margin: 5% auto;
            padding: 20px;
            border: 1px solid #ddd;
            width: 80%;
            max-width: 600px;
            border-radius: 10px;
            color: white; 
        }

       
        .close {
            color: white;
            float: right;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
        }

        .close:hover,
        .close:focus {
            color: red; 
            text-decoration: none;
        }

       
        .modal-header {
            font-size: 1.8rem;
            font-weight: bold;
            color: red; 
            margin-bottom: 15px;
        }

        .team-list {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .team-item {
            display: flex;
            justify-content: space-between; 
            background-color: #444; 
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
        }
        .nav-item img {
        height: 47px;
        width: 47px;
        object-fit: contain;
        margin-bottom: 5px;
    }

        .team-item:hover {
            background-color: #555; 
            cursor: pointer;
        }

        .team-item .team-name {
            font-size: 1.4rem;
            font-weight: bold;
            color: white; 
        }

        .team-item .team-name:hover {
            color: red; 
        }
        
        .ajouter-button {
            background-color: red;
            color: white;
            border: none;
            padding: 5px 10px;
            font-size: 0.9rem;
            border-radius: 5px;
            cursor: pointer;
        }

        .ajouter-button:hover {
            background-color: #cc0000;
        }
</style>
<body>
     <!--Navbar-->
     <%- include('composant/navbar') %>
     <!--Navbar-->
     <div class="nav">
        <% if (games && games.length > 0) { %>
            <% games.forEach(dateEntry => { %>
                <% dateEntry.games.forEach(game => { %>
                    <div class="nav-item">
                        <div class="game-time"><%= game.formattedStartTime %></div>
                        <div class="team">
                            <img style="height:43px" src="<%= game.awayTeam.logo %>" alt="<%= game.awayTeam.name %>">
                            <div class="team-abrrev"><%= game.awayTeam.abbrev %></div>
                            <div class="team-score"><%= game.awayTeam.score %></div>
                        </div>
                        <div class="team">
                            <img src="<%= game.homeTeam.logo %>" alt="<%= game.homeTeam.name %>">
                            <div class="team-abrrev"><%= game.homeTeam.abbrev %></div>
                            <div class="team-score"><%= game.homeTeam.score %></div>
                        </div>
                    </div>
                <% }) %>
            <% }) %>
        <% } else { %>
            <div class="nav-item">aucun match en cours</div>
        <% } %>
    </div>
     <div class="mt-4 container">
    <h1>Bienvenue, <%= username %>!</h1> 
    <p>Personnaliser votre page</p>
    <p>Choisissez votre équipe favorite <a id="fav" href="javascript:void(0);"><i>ici</i></a></p>

    <a href="http://localhost:3000" id="retour-accueil" class="left-align">Retour à l'accueil</a>
            <!-- Modal -->
<div id="favModal" class="modal">
        
    <div class="modal-content">
      <span class="close" id="closeModal">&times;</span>
      <div class="modal-header">
        <h2>Les Equipes</h2>
      </div>
      <div class="team-list">
       <!-- LIST OF TEAMS -->
        <div class="team-item">
          <div class="team-name">Sabres de Buffalo</div>
          <button class="ajouter-button">Ajouter</button>
        </div>
  
        <div class="team-item">
          <div class="team-name">Panthers de la Floride</div>
          <button class="ajouter-button">Ajouter</button>
        </div>
  
        <div class="team-item">
          <div class="team-name">Lightning de Tampa Bay</div>
          <button class="ajouter-button">Ajouter</button>
        </div>
  
        <div class="team-item">
          <div class="team-name">Maple Leafs de Toronto</div>
          <button class="ajouter-button">Ajouter</button>
        </div>
  
        <div class="team-item">
          <div class="team-name">Bruins de Boston</div>
          <button class="ajouter-button">Ajouter</button>
        </div>
      </div>
    </div>
  </div>
  <%- include('composant/footer') %>
  <script>
      
      var modal = document.getElementById("favModal");
  
           var favButton = document.getElementById("fav");
  
     
      var closeModal = document.getElementById("closeModal");
  
     
      favButton.onclick = function() {
          modal.style.display = "block";
      }
  
      
      closeModal.onclick = function() {
          modal.style.display = "none";
      }
  
      
      window.onclick = function(event) {
          if (event.target == modal) {
              modal.style.display = "none";
          }
      }
      
  </script>
  
  
  </body>
  </html>